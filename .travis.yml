language: node_js
node_js:
  - "node"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

install:
  - npm install

script:
  - ng build --prod

notifications:
  email:
    on_failure: change
    on_success: change
